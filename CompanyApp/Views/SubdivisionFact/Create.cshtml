@model SubdivisionFact

@{
    ViewData["Title"] = "Create";
}

<h1>Создание записи</h1>

<h4>Выполненный план работ предприятия</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="subdivisionFactDate"  class="control-label">Дата выполненной работы</label>
                <input asp-for="subdivisionFactDate" class="form-control" />
                <span asp-validation-for="subdivisionFactDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="subdivisionFactIndex" class="control-label">Рейтинг предприятия</label>
                <input asp-for="subdivisionFactIndex" class="form-control" />
                <span asp-validation-for="subdivisionFactIndex" class="text-danger"></span>
            </div>
             <div class="form-group">
                <label asp-for="subdivisionId" class="control-label">Название предприятия</label>
                @Html.DropDownListFor(model => model.subdivisionId, ViewBag.subdivisions as SelectList)
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Вернутся к списку</a>
</div>
